package classSix;

public class ClassOne {
	//The access modifiers are public, default, protected and private
	//public: we can have access to it from all class and packages inide the project.
	//default: we can have access to it from just same classes and same package sub-classes and same package non sub-classes
	//protected: we can have access to it from same classes and same package sub-classes and same package non sub-classes
	// and different package sub classes.
	//private: we can have access to it just from same class.

	public static void main(String[] args) {
		ClassTwo c = new ClassTwo();

	}

}
